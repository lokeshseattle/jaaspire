import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const ImageEditing = () => {
    return (
        <View>
            <Text>ImageEditing</Text>
        </View>
    )
}

export default ImageEditing

const styles = StyleSheet.create({})